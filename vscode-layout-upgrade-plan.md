# Scratch 编辑器「VS Code 布局」硬核化升级方案

> 目标：在开启「高级设置 → 布局 → VS Code 布局」后，让整个编辑器从“长得像 VS Code”升级为“用起来像 VS Code”，
> 提供键盘驱动、可定制、可扩展的硬核开发体验。
>
> 文档性质：**方案与路线图**（不含具体实现代码）。各条目标注优先级（P0/P1/P2）与依赖模块。

---

## 0. 现状盘点

已具备的基础设施：

| 能力 | 现状 |
| --- | --- |
| VS Code 布局开关 | `EnableVSCodeLayout`（`AESettings`，localStorage `AESettings`） |
| 活动栏 | 左侧 41px 图标栏：标签 + 工具按钮（可开关/排序，`mw:activity-bar-order/hidden`）+ 底部固定「登录/头像 + 高级设置」 |
| 菜单栏 | `menu-bar-layout.jsx` 可配置（`mw:menu-bar-order/hidden`） |
| 状态栏 | `EnableStatusBar` 开关已有 |
| 代码编辑器 | 超级重构内的 Monaco（iframe） |
| 扩展 | 插件设置（Puzzle 按钮）、Live Collaboration、Git、书签、AI Agent、Todo、成就 |
| 事件总线 | `window.dispatchEvent(new Event('rw-*'))` 模式 |

**核心差距**：目前只有“壳”（布局条），缺少 VS Code 的**交互范式**（命令面板、面板系统、工作区树、主题、快捷键体系）。

---

## 1. 总体架构：五大功能区

```
┌──────────────────────────────────────────────────────────────┐
│ ① 标题栏 / 命令中心（可选，产品名 + 搜索框）                  │
├────────┬─────────────────────────────────────────────────────┤
│ ②菜单栏 │                                                      │
├────────┼──────────────┬──────────────────────────────────────┤
│ ③活动栏 │ ④ 侧边栏(资源树) │       主编辑区（Monaco/积木区）      │
│        │              ├──────────────────────────────────────┤
│        │              │ 面板栏(⑤) 问题/终端/输出/控制台/调试      │
│        ├──────────────┴──────────────────────────────────────┤
│        │  ⑥ 状态栏（分支 / 错误数 / 光标位置 / 缩进 / 编码）    │
└────────┴─────────────────────────────────────────────────────┘
```


- ①③ 属“壳”，②④⑤⑥ 属“血”。
- 全部功能区由新的 `mw:layout-state.js` 统一管理（显示/隐藏/尺寸持久化）。

---

## 2. 功能方案明细

### 2.1 命令面板（最高优先级 · 硬核核心）—— P0

> 一句话：一切操作皆可搜索、键盘直达。

- 快捷键 `Ctrl+Shift+P`（命令）与 `Ctrl+P`（快速打开文件）复用同一浮层。
- 命令注册表 `mw:commands.js`：
  - 内置：切换布局开关、切换面板、切换主题、新建/保存项目、打开插件设置、打开 Git、切换书包、打开超级重构、运行全部积木、停止全部积木……
  - 插件命令：插件设置中新增“注册命令”接口。
- 浮层样式完全复刻 VS Code：顶部输入框、分组列表、关键词高亮、上下键选择、回车执行、`Esc` 关闭、支持中文模糊匹配。
- 依赖：新增 `src/lib/mw-command-palette.js` + `src/components/command-palette/`。

### 2.2 面板系统（底部 / 侧边面板）—— P0

| 面板 | 功能 | 说明 |
| --- | --- | --- |
| 问题（Problems） | 收集积木脚本错误、缺失资源、云变量冲突 | 红色波浪纹 → 点击跳转到对应积木 |
| 控制台（Console） | `console.log` 重定向 + 扩展输出 | 对接 scratch-vm 运行时日志 |
| 输出（Output） | 项目保存/导出/上传日志 | 类似构建输出 |
| 终端（Terminal） | 可选，xterm.js 接入扩展沙箱 | P2，可延后 |
| 调试（Debug） | 变量监控、单步、断点（对积木） | P1，联动超级重构 |

- 面板栏复用 `menu-bar-layout` 的开关/排序模式，新增 `mw:panel-layout`。
- 每个面板独立“面板图标”进活动栏，点开切换、可停靠到底部或右侧。

### 2.3 侧边栏：工作区资源树（Explorer）—— P0

- 用“项目资源”替代文件树：
  - 精灵/舞台/造型/声音/变量/自制积木/后台脚本/书签/云端文件。
  - 树形展开/折叠、拖拽排序、右键菜单（重命名、复制、删除、导出、上传）。
- 依赖现有 `getProjectFiles`（超级重构的 id 化文件列表）改造为通用 `mw:workspace-tree.js`。
- 配合活动栏新增“资源管理器”图标（Files/ListTree）。
- 半透明浮动式（类似当前书包的 `vscode-backpack-host` 模式），不与积木区互相遮挡。

### 2.4 标题栏 + 命令中心 —— P1

- 顶部：面包屑标题（项目名 / 分支 / 布局模式徽标）。
- 可选“命令中心”输入框（点击聚焦、`Ctrl+P` 直达），弱化传统菜单栏、强化键盘流。

### 2.5 状态栏全面强化 —— P1

在现有 `EnableStatusBar` 基础上扩展，从左到右：

```
[分支:main] [←→ 空格:4] [行:12 列:34] [UTF-8] [LF] [积木模式] [✓ 0错误 0警告] [雷真] 
```

- 点击“错误数”直达问题面板；点击“行列”聚焦到代码编辑器。
- 项目/平台信息放右侧，与 VS Code 的“通知/云端/设置齿轮”对齐。

### 2.6 代码编辑器（Monaco）硬核化 —— P1

超级重构内的 Monaco 增加：

- **Minimap** 缩略图、**面包屑**（文件→精灵→造型→脚本）、代码折叠、多光标（`Alt+Click`）、行号、括号匹配。
- **问题标记**：语法错误红波浪线 + 悬停提示。
- **快捷键**：`Ctrl+F` 查找、`Ctrl+/` 注释、`Ctrl+Space` 提示（积木语义）。
- 深色/浅色随主题联动。
- 顶部 tab 条支持多文件多开、拖拽分屏（`Ctrl+\` 垂直分割）。

### 2.7 主题与图标系统 —— P1

| 类别 | 方案 |
| --- | --- |
| 颜色主题 | 内置 4 套：`深色+`（默认硬核）、`浅色`、`高对比`、`One Dark`；同步 4 个功能区配色 |
| 图标主题 | 活动栏/资源树图标包（lucide 现有资源 + 新增 svg 包），支持用户自选 |
| 背景 | 支持自定义编辑器背景图 + 透明度（P2） |

### 2.8 Git 集成可视化 —— P1

- 活动栏 Git 按钮 → 侧边“源代码管理”面板：改动列表（绿色新增/红色删除徽标）、暂存/提交、分支切换、冲突标记。
- 与现有 `openGitModal` 打通，树形展示 diff。
- 状态栏常驻分支徽标。

### 2.9 快捷键体系 + 快捷键设置界面 —— P2

- 全部动作注册进 `mw:keybindings.js`，可冲突检测、可改键。
- “快捷键参考”面板（`Ctrl+K Ctrl+S`）按分类列出，支持搜索。
- 提供“经典 Scratch”与“VS Code”两套预设一键切换。

### 2.10 Zen 模式与专注 —— P2

- `Ctrl+K Z`：隐藏活动栏/侧边栏/面板/状态栏，只留主编辑区，`Esc Esc` 退出。
- 与 `EnableMobileLayout`、窗口缩放互不冲突。

### 2.11 多根工作区 / 项目切换器 —— P2

- 复用 `mw:backpackHeight` 式持久化，保存多套“布局快照”（功能区显隐 + 尺寸），一键切换。

---

## 3. 分阶段实施路线

| 阶段 | 内容 | 交付物 | 周期参考 |
| --- | --- | --- | --- |
| **阶段 A（血）** | 命令面板 + 面板系统（问题/控制台）+ 状态栏强化 | 键盘可完成 80% 操作 | 1 期 |
| **阶段 B（肉）** | 资源树侧边栏 + Monaco 增强（minimap/多光标/面包屑） | 多文件硬核编辑体验 | 2 期 |
| **阶段 C（皮）** | 主题系统 + 图标包 + 标题栏/命令中心 | 视觉 100% VS Code | 3 期 |
| **阶段 D（野）** | Git 面板、快捷键设置、Zen、多工作区 | 完整生态 | 4 期 |

---

## 4. 技术要点与风险

### 4.1 复用既有模式（避免重复造轮子）

- **配置持久化**：沿用 `mw:*-order` / `mw:*-hidden` + change event 模式（`mw-activity-bar-changed` 等），新增统一 `mw:layout-state.js`。
- **功能区条件显示**：沿用 settings-modal 的 `condition` 机制与 `pageConfigurations`。
- **浮层避让**：沿用 `vscode-backpack-host`（`left:41px` + `pointer-events`）思路，保证任何浮层不遮挡活动栏。
- **Monaco**：沿用 super-refactor 的 iframe + `LARGE_CONTENT_THRESHOLD` 降级策略。

### 4.2 风险与对策

| 风险 | 对策 |
| --- | --- |
| CSS Modules 类名哈希导致选择器不匹配（`tab-list.vscode` 教训） | 所有新样式只写在实际生效的类（`.vscode`/`.vscodeList`）上，或用 `[name]_[local]_[hash:base64:5]` 命名验证 |
| 书包/面板互相遮挡（z-index 冲突） | 统一 `--mw-zindex` 层级规范：活动栏底栏 1000 > 浮层 100 > 主内容 |
| 面板与积木区抢高度（历史上“撑长页面”问题） | 面板一律 absolute/fixed + `min-height:0` + `overflow`，禁止参与 flex 撑高 |
| 命令面板与本地化 | 命令元数据带 `id`，文案走 `FormattedMessage`，中文模糊匹配 |
| 性能（大项目资源树/大代码文件） | 复用 `LARGE_CONTENT_THRESHOLD` 降级 + 虚拟滚动（`itemsScroller` 模式） |

### 4.3 硬核度评分（自检清单）

- [ ] `Ctrl+Shift+P` 能搜到并执行“所有”动作
- [ ] 问题面板能精确跳转积木
- [ ] 状态栏能一键直达面板/编辑器
- [ ] 全键盘可完成：开项目→改脚本→查错误→提交 Git→切主题
- [ ] 任何浮层/面板不遮挡活动栏与底部固定按钮

---

## 5. 建议启动顺序（最小闭环）

1. **命令面板**（P0，独立模块，收益最大）
2. **问题面板 + 控制台**（P0，配合命令面板形成“操作闭环”）
3. **资源树侧边栏**（P0，让左侧栏真正“像 VS Code”）
4. **状态栏强化**（P1，低成本高感知）
5. 其余按阶段推进。

> 每个模块独立可回滚：新增 `mw:*` 模块 + settings 开关，不影响现有功能与既有持久化键。