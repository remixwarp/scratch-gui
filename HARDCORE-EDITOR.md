# 让编辑器变得更硬核 · 进阶路线图

> 面向高级开发者。这份文档不讲"已经有什么"，只讲"还能往哪走"。
> 目标是把这套编辑器，从"能用的积木台"推到"像 VS Code 一样的工作台"。
> 这里不讨论任何内部实现，只说每个方向能带来的**效果**——你读完应该清楚：还差哪几块，才能让它真正硬核起来。

---

## 进度速览

> ✅ 已实现　🟡 部分 / 进行中　⬜ 规划中

| # | 方向 | 状态 | 备注 |
|---|------|------|------|
| 1 | Zen 模式 | ✅ | 活动栏按钮 + 命令面板入口，一键免打扰 |
| 2 | 命令中心 | ✅ | 标题栏命令面板入口 |
| 3 | 面包屑 | ✅ | 已嵌入代码编辑器顶部（Monaco breadcrumbs + 吸顶滚动 + 缩进参考线） |
| 4 | 多工作区 | 🟡 | 基础架构在 |
| 5 | 底部面板补全 | 🟡 | 输出 + 控制台已落地 |
| 6 | Monaco 满配 | ✅ | 连字 / 多光标 / 滚轮缩放 / 折叠 / 小地图 / 面包屑 |
| 7 | 标题栏强化 | ✅ | 项目名 + 未保存圆点 + Git 分支 |
| 8 | 快捷键深度化 | ✅ | 新增「超级重构」分类与改键 |
| 9 | 调试增强 | 🟡 | 基础断点 / 单步在 |
| 10 | 分屏增强 | 🟡 | 多代码分组在 |

---

## 1. Zen 模式 —— 关掉整个世界

需要一个一键进入的"专注模式"：

- 一键隐藏所有干扰（侧栏、面板、状态栏），只留舞台和你正在编辑的东西。
- 进入后界面极简，眼睛和思路都聚焦在一件事上。
- 再一键随时退出，回到完整界面。

效果：**需要深度专注时的"免打扰开关"**，把杂音关在门外。

---

## 2. 命令中心（Command Center）—— 标题栏的心脏

在标题栏中央放一个常驻的"输入框"：

- 它既是命令面板的快捷键入口，也能显示当前文件 / 当前上下文。
- 点一下就出命令面板，不用再记 `Ctrl+Shift+P`。
- 配合面包屑，标题栏变成"我在哪 + 我要去哪"的总控台。

效果：**把命令面板的入口钉在最顺手的地方**，随手一敲就能发号施令。


---

## 3. 面包屑（Breadcrumbs）—— 你站在哪一层 ✅ 已落地

在编辑器顶部显示当前位置的层级路径：

- 角色 → 造型 / 脚本 / 声音，一层层展开成可点的路径。
- 点任意一层直接跳过去，不用层层翻 Tab。
- 大型项目里，随时知道"我现在改的是哪个角色的哪一块"。

效果：**永远知道自己站在项目结构的哪一层**，不再迷路。

> 已实现：代码编辑器（Super Refactor 的 Monaco 实例）顶部已嵌入面包屑，并附带吸顶滚动（sticky scroll）与缩进参考线，长脚本里始终知道"我现在在哪一层"。

---

## 4. 多工作区 / 项目切换器 —— 同时驾驭多个项目

需要一个"项目根"的概念和切换器：

- 可以并排打开多个项目，每个都有独立的布局与上下文。
- 顶部一个切换器，像翻书一样在不同项目间跳。
- 跨项目搬运、对照、复用，不用反复打开关掉。

效果：**多任务场景的"驾驶舱"**，为真正同时跑多个作品的人而生。

---

## 5. 底部面板补全：输出 + 终端

现在的底部面板只有"问题"和"控制台"，还差两块：

- **输出面板**：把构建、编译、扩展运行等各种来源的输出分频道归置，想看哪路看哪路。
- **终端面板**：把命令行直接嵌进底部面板栏（而不是独立弹窗），随用随收，不占额外窗口。

效果：**底部面板变成真正的"多频道控制台"**，所有运行时信息一个地方管完。

---

## 6. Monaco 满配 —— 把代码编辑器逼到极限

代码工作区（Fractch）还能更像专业 IDE：

- **字体连字（Ligatures）**：`=>`、`!=` 这类符号渲染成连笔，读代码更顺眼。
- **多光标编辑**：在多处同时落光标，批量改名字、批量改值一气呵成。
- **滚轮缩放**：按住 `Ctrl` 滚鼠标直接放大缩小字号。
- **代码折叠**：把大段脚本收起来，只看骨架，需要时再展开。
- **可选小地图（Minimap）**：右侧一个缩略图，长文件里快速定位。
- **面包屑联动**：上面第 3 条的面包屑直接嵌进编辑器顶部。

效果：**代码编辑体验向"专业 IDE"看齐**，写脚本像写源码一样顺。

---

## 7. 标题栏强化 —— 信息 + 控制合一

标题栏不该只是装饰：

- 显示当前项目名、Git 分支、有无未保存改动（一个小圆点就够）。
- 配合命令中心（第 2 条）和面包屑（第 3 条），标题栏成为"状态 + 导航 + 控制"的总入口。
- 可配置显隐，极简党和信息党各取所需。

效果：**一眼看出项目状态，所有高频操作抬手就到。**

---

## 8. 快捷键深度化 —— 连"操作序列"也能绑定

现在的快捷键已经能改键，还能更狠：

- 支持"宏"：把一个多步操作序列绑到一个键上。
- 支持按上下文自动切换键位（在代码区、在舞台区用同一套键做不同的事）。
- 导入 / 导出键位方案，换机器一键还原你的肌肉记忆。

效果：**把"重复操作"压缩成"一个键"**，高频流程彻底固化进指关节。

---

## 9. 调试增强 —— 断点与变量窥视

现在的调试窗口能暂停单步，还能更硬核：

- 在脚本里直接打**断点**，运行到那一行自动停。
- 实时**窥视变量**：哪个变量现在是什么值，悬停或开个小窗就看到。
- 调用栈：当前这行脚本是被谁触发、从哪一层进来的，一目了然。

效果：**从"逐步看"升级成"定点看"**，复杂逻辑排错时间砍半。

---

## 10. 分屏增强 —— 跨视图并排

现在的分屏已经能多开代码分组，还能延伸到整个编辑器：

- 把舞台和代码并排，改一下看一眼，不必来回切。
- 多个角色的代码左右对照，复制逻辑更顺。
- 布局可存成"快照"，下次一键还原你最顺手的那套排布。

效果：**多个视角同时在线**，对照、重构效率拉满。

---

## 结语

这份清单不是"待办"，而是一张**坐标图**：标出从"能用"到"硬核"之间，还差哪几块。

每一项都指向同一种体验——**让做东西的人，拥有跟写代码的人一样的掌控感与速度**。

不一定要一次做完。挑最戳你痛点的那一两块，先动起来，编辑器就会一点一点，长成你手里那把趁手的刀。

—— 硬核，是一条没有终点的路。
